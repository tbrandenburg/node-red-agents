{
  "name": "node-red-contrib-agent-server",
  "version": "0.0.1",
  "description": "Long-lived opencode serve daemon management node (session-based, SRT-sandboxable), companion to node-red-contrib-agent's one-shot execution model.",
  "keywords": ["node-red", "agent", "opencode", "ai", "llm", "server"],
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {},
  "node-red": {
    "nodes": {
      "agent-server": "agent-server.js"
    }
  }
}
