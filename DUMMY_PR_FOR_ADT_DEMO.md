# Dummy PR — ADT demo fixture

This is a throwaway file added solely to create a real, open pull request
so the "Agentic Development Team" demo tab's PR Review sub-workflow could
be exercised end-to-end for the first time against a genuine PR (it had
previously only ever run against merged/closed PRs, never a live open one).

This PR is expected to be closed without merging once the demo run that
needed it is complete. Safe to delete.
